#include <stdio.h>

int main(void)
{
  int income,outgo,balance;     // 宣告變數而不給予初始值 
  printf("收入 %d\n",income);   // 將變數值從螢幕輸出 
  printf("支出 %d\n",outgo);
  printf("結餘 %d\n",balance);

  return 0;
}
